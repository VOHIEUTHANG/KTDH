import draw3DCoordiante from "./draw_3D_coordinate";
import { drawLine } from "./draw_functions";
export default function draw_3D(ctx, rootPoint, FRAME_WIDTH, FRAME_HEIGHT) {
  draw3DCoordiante(ctx, rootPoint, FRAME_WIDTH, FRAME_HEIGHT);
}
